<h1>Listing monthly_activity_summaries</h1>

<table>
  <tr>
  </tr>

<% for monthly_activity_summary in @monthly_activity_summaries %>
  <tr>
    <td><%= link_to 'Show', monthly_activity_summary %></td>
    <td><%= link_to 'Edit', edit_monthly_activity_summary_path(monthly_activity_summary) %></td>
    <td><%= link_to 'Destroy', monthly_activity_summary, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New monthly_activity_summary', new_monthly_activity_summary_path %>
